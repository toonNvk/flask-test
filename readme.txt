windows

pip install virtualenv
create folder
pip uninstall virtualenv
pip install virtualenv
pip install virtualenvwrapper-win
mkvirtualenv [folder-project-name]
setproject .
deactivate
workon [folder-project-name] // like activate and work on it
pip install flask 
// we create vm for python project and we must install lib for work on this project
